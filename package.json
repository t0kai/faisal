{
  "name": "abdullah-faisal-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "notion:ids": "node scripts/list-datasources.mjs",
    "contrast": "node scripts/validate-contrast.mjs",
    "preflight": "node scripts/preflight.mjs",
    "prebuild": "node scripts/preflight.mjs"
  },
  "dependencies": {
    "@notionhq/client": "^5.24.0",
    "next": "^15.5.0",
    "next-intl": "^4.3.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "resend": "^6.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "postcss": "^8.5.26",
    "sharp": "^0.35.3"
  },
  "//overrides": "Next 15.5.x pins older postcss and sharp that carry HIGH advisories; the upstream fix only lands in Next 16 (semver-major). These overrides pull the patched releases instead. Verified: npm audit \u2192 0 vulnerabilities, build passes. Remove them when moving to Next 16."
}
